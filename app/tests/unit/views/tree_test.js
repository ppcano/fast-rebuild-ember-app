/*
var run = Ember.run;
moduleFor('view:tree', 'app-person', {
  teardown: function() {

    console.log('app-person.setup:');
    if ( App ) { 
      console.log('app-person.reset ....');
      App.reset();
    }
  }
});

test('can access to component property', function() {
  expect(1);
  var component = this.subject();
  equal(component.get('name'), 'ppcano');
});
*/
